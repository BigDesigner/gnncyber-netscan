# Sprint Pipeline

This document tracks active tasks and backlogs for GNNscan development.

---

## Active Sprint Tasks

- [ ] Core Shell Setup: Implement `lib/main.dart` with local `HttpServer` and WebView channel bindings.
- [ ] Scanning Engine: Implement `lib/scan_engine.dart` with async TCP connect scan and banner grabs.
- **Progress:** Not started.

---

## Sprint Backlog

- [ ] History & Configuration DB: Implement JSON data manager in `lib/history_db.dart`.
- [ ] Inno Setup: Create compilation installer script `setup.iss`.
- [ ] CI/CD Pipeline: Add GitHub Actions workflow in `.github/workflows/build.yml`.
- [ ] Repository Launch: Initialize local git repository, commit, and push.

---

## Long-term Backlog
- [ ] Local Subnet interface detection using native platform code.
- [ ] ICMP Ping option (with admin privileges fallback).
